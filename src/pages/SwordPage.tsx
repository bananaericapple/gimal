import React from 'react';
import SwordApp from './sword/App';

const SwordPage: React.FC = () => (
  <div className="min-h-[calc(100vh-var(--topbar-h))] w-full">
    <SwordApp />
  </div>
);

export default SwordPage;
